# 云端访问部署说明

这个审计控制台本质上是一个 Web 服务，默认监听：

```text
0.0.0.0:3000
```

所以它可以部署在云服务器上，并通过公网 IP、域名或临时隧道生成一个可以转发给学员的网页链接。

## 方式一：直接用云服务器 IP 访问

在云服务器上进入项目目录：

```bash
cd "/path/to/Security Guardian"
python -u atlas_lab/app.py
```

如果服务器的公网 IP 是：

```text
1.2.3.4
```

并且安全组已经开放 `3000` 端口，那么访问链接就是：

```text
http://1.2.3.4:3000
```

需要检查：

- 云服务器安全组放行 TCP `3000`
- 系统防火墙放行 TCP `3000`
- 服务启动时没有只绑定 `127.0.0.1`

## 方式二：用域名和 Nginx 反向代理

更推荐这种方式。

假设域名是：

```text
audit.example.com
```

Python 服务仍然运行在本机：

```text
127.0.0.1:3000
```

Nginx 示例配置：

```nginx
server {
    listen 80;
    server_name audit.example.com;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

配置后访问：

```text
http://audit.example.com
```

如果已经配置 HTTPS，可以访问：

```text
https://audit.example.com
```

## 方式三：临时公网转发链接

如果只是课堂临时演示，可以使用隧道工具把本地 `3000` 端口转发出去。

常见形式类似：

```bash
cloudflared tunnel --url http://localhost:3000
```

或：

```bash
ngrok http 3000
```

这类工具会生成一个临时公网链接，适合短时间课堂演示。

## 安全提醒

如果只是课程演示，可以使用模拟日志和模拟数据。

如果接入真实云服务器上的 OpenClaw，请注意：

- 不要在页面上展示真实 API Key
- 不要展示真实 SSH 私钥
- 不要公开真实客户数据
- 不要把审计控制台长期裸露在公网
- 建议使用 Nginx Basic Auth、IP 白名单或 VPN 保护
- 建议只给 Claude Code 提供脱敏后的日志和配置快照

## 推荐课堂方案

课堂转发链接推荐：

```text
https://audit.your-domain.com
```

架构：

```text
学员浏览器
  ↓
域名 / HTTPS / Nginx
  ↓
127.0.0.1:3000
  ↓
OpenClaw Claude Code 安全自审计服务
```

这样比直接暴露 `http://服务器IP:3000` 更稳定，也更像真实企业部署。

## 和课程 14 相同的启动方式

如果使用和 `Morning-Newspaper-Assistant` 类似的端口转发方式，可以运行：

```powershell
.\run_dashboard.cmd
```

它会默认监听：

```text
127.0.0.1:8511
```

然后把 `8511` 端口交给你的云服务器控制台、IDE 端口转发、内网穿透或反向代理工具。

这和课程 14 的 `run_dashboard.cmd` 思路一致：服务只监听本地地址，由外层工具负责生成可分享链接。

如果云服务器安全组直接开放 `8511`，链接可以写成：

```text
http://101.47.152.44:8511/dashboard.html
```

本项目建议固定使用：

```text
8511
```

修改 `run_dashboard.cmd` 里的：

```cmd
set ATLAS_PORT=8511
```

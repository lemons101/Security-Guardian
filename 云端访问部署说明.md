# 云端访问部署说明

Security Guardian 是一个只读审计 Web 服务，推荐固定监听：

```text
0.0.0.0:8511
```

公网访问目标：

```text
http://101.47.152.44:8511/dashboard.html
```

## 直接公网访问

云服务器执行：

```bash
cd /root/projects/Security-Guardian
chmod +x run_dashboard.sh
OPENCLAW_ROOT=/root/projects/OpenClaw ./run_dashboard.sh
```

需要确认：

- 云安全组放行 TCP `8511`
- 系统防火墙放行 TCP `8511`
- 服务监听地址是 `0.0.0.0:8511`
- `OPENCLAW_ROOT` 指向真实 OpenClaw 目录

## Nginx 反向代理

如果不想直接暴露端口，可以让服务只在内网运行，再用 Nginx 暴露域名。

示例：

```nginx
server {
    listen 80;
    server_name audit.example.com;

    location / {
        proxy_pass http://127.0.0.1:8511;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

## 安全提醒

- 这个页面会展示 OpenClaw 审计结果，不建议长期裸露公网。
- 生产环境建议加 IP 白名单、Basic Auth、VPN 或企业 SSO。
- 审计包应使用脱敏日志和配置快照。
- 不要输出真实 API Key、Token、SSH 私钥或客户明文数据。
- Security Guardian 只生成检测报告和建议，不自动修改 OpenClaw 生产配置。

## Windows 本地启动

```powershell
cd "D:\Openclaw\Security Guardian"
.\run_dashboard.cmd
```

本地访问：

```text
http://127.0.0.1:8511/dashboard.html
```

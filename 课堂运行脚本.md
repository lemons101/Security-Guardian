# Claude Code 审查云端 OpenClaw 安全治理课堂运行脚本

## 课前准备

启动审计控制台：

```powershell
.\run-conda-env1.bat
```

或：

```powershell
.\run-local.bat
```

打开：

```text
http://localhost:3000
```

## 任务 0：理解实验场景

场景不是“攻击一个靶场”，而是：

> OpenClaw 已经部署在云服务器上。现在 OpenClaw 主动调用 Claude Code，对自己的日志、配置快照、Skill 权限、Token 用量和治理策略进行安全自审计。

学员先查看：

- 云端 OpenClaw 状态
- 云端日志与监控告警
- OpenClaw 治理状态总览

初始风险包括：

- WebSocket 监听在 `0.0.0.0:7070`
- 控制面缺少强鉴权和 Origin 校验
- Token 出现在访问日志中
- 社区 Skill 未签名校验
- Skill 请求读取密钥和财务目录
- Token 用量短时间暴涨
- denyList 与 Token 熔断未启用

## 任务 1：OpenClaw 调用 Claude Code 分析云端日志

点击：

```text
1. 分析云端日志
```

Claude Code 会输出结构化风险报告：

```text
CC-001｜严重｜位置：cloud.configSnapshot.websocketBind
证据：websocketBind=0.0.0.0:7070
建议：限制绑定地址、启用强鉴权、校验 Origin

CC-002｜严重｜位置：openclaw-control logs
证据：Access token observed in websocket query string
建议：吊销旧 Token，改用短期 Bearer Token

CC-004｜严重｜位置：skill-runner / egress-proxy
证据：reconcile-plus requested /secrets and attempted POST
建议：隔离 Skill，禁止读取 secrets / finance，默认拒绝出站
```

学员要观察的是：

- Claude Code 是否指出了风险位置
- 是否给出证据
- 是否说明影响
- 是否给出处置顺序

## 任务 2：启用 Claude Code 监控告警

点击：

```text
2. 启用监控告警
```

预期出现：

```text
control-plane-token-in-url
skill-secret-egress-chain
token-usage-spike
```

教学点：

> 一次性审计用于上线前判断，持续监控用于生产运行期间发现异常。

## 任务 3：根据报告封堵控制面

点击：

```text
3. 封堵控制台
```

系统会将：

- 控制台强鉴权：ON
- Origin 校验：ON
- 远程关闭安全策略：禁止
- 会话 TTL：30 分钟

教学点：

> 控制面是数字员工的“遥控器”，必须先治理。

## 任务 4：根据报告隔离社区 Skill

点击：

```text
4. 隔离 Skill
```

系统会将：

- Skill 沙盒：ON
- Skill 文件系统：只读
- 禁止读取 `secrets`
- 禁止读取 `finance`
- 出站网络：默认拒绝

教学点：

> 不需要证明 Skill 已经作恶。只要它是社区来源、请求敏感目录、存在外传迹象，就应该先限制爆炸半径。

## 任务 5：密钥轮换与强鉴权

点击：

```text
5. 密钥轮换
```

系统会执行：

- 吊销旧 Access Token
- 吊销旧业务 API Key
- 签发短期 Token
- 敏感操作需要二次确认

教学点：

> Token 出现在日志里，就按泄露处理。

## 任务 6：denyList 与 Token 熔断

点击：

```text
6. denyList / 熔断
```

系统会启用：

- 高危命令 denyList
- 敏感文件 denyList
- 单任务 Token 上限
- 每日 Token 上限
- 超限暂停
- 完整审计日志

教学点：

> 生产治理不只防外部攻击，也防数字员工自己失控。

## 任务 7：最终越权审计

点击：

```text
7. 最终越权审计
```

预期结果：

```text
越权审计：8/8 通过
上线结论：允许进入受控上线
```

最终产出：

- Claude Code 云端日志审计报告
- Claude Code 监控告警
- OpenClaw 治理动作记录
- 最终越权审计结果
- 上线结论

## 课堂收口

让学员基于页面结果填写：

```text
checklists/OpenClaw生产上线安全核查表.md
```

结论必须是三选一：

- 允许进入受控上线
- 暂缓上线
- 禁止上线

本节课的核心：

> OpenClaw 调用 Claude Code 审查自己的安全状态，Claude Code 输出风险报告，Security Guardian 执行治理，最终用越权审计决定是否允许进入无人值守生产运行。
